/**
 * useMetronome — manages Tone.js Transport lifecycle for the metronome.
 * Audio context is created lazily on first play (browser autoplay policy).
 */
export function useMetronome() {
  // TODO: implement Tone.js Transport integration
  return {}
}
