/**
 * usePracticeTimer — stopwatch using requestAnimationFrame for smooth updates.
 * Drives the elapsed seconds display in the active practice session.
 */
export function usePracticeTimer() {
  // TODO: implement requestAnimationFrame stopwatch
  return {
    elapsedSeconds: 0,
    start: () => {},
    stop: () => {},
    reset: () => {},
  }
}
